<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="BabyStepz.Views.NewItemPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="New Task">
    <ContentPage.ToolbarItems>
        <ToolbarItem Clicked="Save_Clicked" Text="Save" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout
            Padding="15"
            BackgroundColor="{x:DynamicResource PrimaryDarkColor}"
            Spacing="20">
            <Label
                FontSize="Medium"
                Text="Titel"
                TextColor="{x:DynamicResource PrimaryTextColor}" />
            <Entry
                FontSize="Small"
                Text="{Binding Item.Text}"
                TextColor="{x:DynamicResource AltTextColor}" />
            <Label
                FontSize="Medium"
                Text="Description"
                TextColor="{x:DynamicResource PrimaryTextColor}" />
            <Editor
                Margin="0"
                FontSize="Small"
                Text="{Binding Item.Description}"
                TextColor="{x:DynamicResource AltTextColor}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
